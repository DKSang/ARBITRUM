{
  "$schema": "https://app.kilo.ai/config.json",
  "mcp": {
    "chrome": {
      "type": "local",
      "command": ["npx", "chrome-devtools-mcp@latest"],
      "enabled": true,
      "timeout": 60000
    }
  },
  "permission": {
    "chrome_*": "allow"
  }
}
